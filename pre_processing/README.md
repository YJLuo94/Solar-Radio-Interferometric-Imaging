# Pre-processing

This module contains scripts for data inspection, data selection, partitioning, and initial flagging before calibration.

Typical scripts:

- `pre_calibration.py`: inspect input Measurement Set, record metadata, select target/calibrator data, and prepare partitions.
- `initial_flag.py`: apply initial manual or automatic flagging before calibration.
